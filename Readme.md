Backend SetUp
npm install
npm run dev
then the server will start on port 4000

Frontend Setup
npm install
npm run dev
then the server will start on port 3000

Try it out locally by going to http://localhost:3000 and enter username and youtube video url

